// This script will be executed when the MongoDB container is created
// It will create the database and collections if they don't exist

print('Start MongoDB initialization');

// Wait for the replica set to be initiated by the mongo-init service
// This is just a placeholder as the actual replica set initialization happens in the mongo-init service
